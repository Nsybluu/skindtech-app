EXPO_PUBLIC_API_URL=http://YOUR_MAC_LAN_IP:3000

# Offline demo fallback when the API cannot be reached.
#   mock  (default) show the sample "moderate acne" result, clearly badged
#   clear            show the sample "no visible acne" result
#   off              show the real Analysis failed screen
EXPO_PUBLIC_SCAN_FALLBACK=mock
